# Runtime dependencies for the Part 107 study app.
# Flask pulls in Werkzeug, Jinja2, and MarkupSafe automatically.
# gunicorn is the production WSGI server named in the Procfile (Linux/macOS hosts).
Flask>=3.0
gunicorn>=21.2
